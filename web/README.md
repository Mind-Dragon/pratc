# prATC Web

Wave 1 scaffold for the prATC dashboard. This task provides a static Next.js shell with:

- a dashboard landing page
- a triage inbox placeholder
- shared layout and navigation
- local vitest coverage for the shell components

Commands:

- `bun install`
- `bun run dev`
- `bun run build`
- `bun run test`
